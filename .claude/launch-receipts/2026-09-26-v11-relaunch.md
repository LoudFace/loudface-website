# Launch receipt — 2026-09-26 — v11 relaunch (every public route)

Scope: every public route switched from its v3 component to the approved v11 component, same URLs, metadata, JSON-LD,
redirects, `revalidate` and `generateStaticParams`. The launch is the 158 entries in the staged file list (the untracked
`dev-proposal` drafts, `public/images/objects`, `design-lab/`, `.paper-import/` and the `home-v3` experiments stay out).
Base commit 94fbed0; launch commit: written when Arnel gives the green light. Route map and decisions: `docs/v11-handoff.md`.

Preview: https://loudface-website-pn80im102-loud-face.vercel.app (2026-09-27, behind Vercel login; supersedes
cvbc9h624 and 44j3n5g1c), built from exactly the launch
files. All 185 sitemap routes 200; unknown URLs, a missing post and `/blog?page=999` 404; `sitemap.xml` (no-store,
180 URLs), `llms.txt`, `robots.txt` 200; zero editor markers (U+E0001) in anonymous HTML on all 186 checked routes.

Lessons loaded: 12 (house 12, client 0); applied: "When you replace an image's CONTENT but keep the same file path"
(avatars and logos were resized in place: never deployed before, served as static files, not through next/image, so no
cache holds an old copy; the photos got new names), "a `sitemap.ts` MetadataRoute cannot serve an uncacheable sitemap"
(route handler kept; preview answers `no-store`), "after a main-push rebuild the FIRST request … returns the OLD cached
HTML" and "Before judging a page … run URL Inspection" (both in the post-launch steps), "publishing in the Sanity Studio
does NOT update the live site by itself" (revalidate route unchanged; the new chart read carries the case study tag it
purges), "A tile in a fluid grid must be sized by aspect ratio" (checked: related and card thumbnails keep their ratio;
browser frames crop a page screenshot from the top by design), "When deciding which page owns a tracked AI prompt"
(the GEO service page the engines cite kept all its text: 2,055 words before, 2,314 after). Not applicable: the PostHog
second project, Intercom inline, Team App brand boards, video short answers, telling a client's engineers about Search Console.

## Changes after the first gate run (2026-09-27)

- Menus: Arnel's feedback (a client under each industry read as a case study link; the icons looked generic) led to
  three directions and two icon styles on the Paper page "Menus · pick one"; he picked menu A with the Isocons
  isometric icons. `NavV11` now reads its data through `getNavV11Data()` (`home-v11/nav-data.ts`), which adds each
  industry's buyer question from that page's own content; the footer carries the icon credit CC BY 4.0 asks for.
- The Toku icon in the Bento and service pages was a Vercel favicon; replaced with Toku's own app icon.
- Phone layout: grids that overflowed at 390px now use `minmax(0, 1fr)`; long headings break; wide article tables scroll.
- The result card's hatch pattern had a fixed id, so a second copy on one page (the CRO hero plus a menu preview) lost
  its fill; the ids are now unique per card (`useId`).
- Re-checked: typecheck, `test:editor` 591/591, `check:editor` 0 FAIL, anonymous HTML carries no editor marks, the menu
  questions and footer credit carry marks for a signed-in editor, production build exit 0, 185 sitemap routes 200 on
  the new preview, the menus open by hover and click on desktop and by tap on a phone with all icons loaded.

## Deterministic check

`check.mjs`: 2 FAIL, 11 WARN, 21 PASS. Build verified separately on the staged launch copy (`next build` exit 0).
- FAIL no-default-palette: all four hits are in `src/app/(proposal)/dev-proposal`, which is not in the launch.
- FAIL sitemap-static-pages: `/audit` and `/audit/demo` are noindex through the `(audit)` layout (header and meta, checked
  on the preview), `/preview-proof` is noindex, and `/dev-proposal/*` is not in the launch. The check cannot see a
  layout's robots setting.
- WARN no-hex-in-markup: the v11 figures (charts, product UI drawn at 1:1) carry their own colours; decorative SVG is the
  tolerated exception. Most hits are in `dev-proposal`.
- WARN focus-visible, link-component, readme-real, no-todos, tracked-artifacts, package-manager-pin, sanity-fetch-ttl,
  sanity-read-budget: present before this change, in files it does not touch (forms, v3 components, careers data,
  page timers on routes that render per request anyway).
- WARN sanity-image-bandwidth: every CMS image on the v11 pages now goes through Vercel's image cache
  (`cachedCmsImage`). What still loads from cdn.sanity.io matches the live site: article-body images from CMS HTML,
  plus SVG logos on /case-studies and three click-to-play testimonial videos (noted in the handoff).
- WARN build: see above.

Also green: `npm run typecheck`, `npm run test:editor` 591/591, `npm run check:editor` 0 FAIL, inline-edit coverage over
one route of every template.

## Gates

- Design CLOSE: each page was designed and reviewed on its Paper board ("LoudFace Website · v11"); the pick log runs to
  2026-09-25. The final human pick is Arnel's review of this preview; log it with `capture_pick.py` when he approves.
- qa-loop: sweep of 205 pages (no internal breakage; the fails are LinkedIn's bot block and three dead external links in
  CMS blog bodies); scripted runtime and axe lanes over 236 loads; interactive lanes A and B; design lanes 1 to 3.
  Every P0/P1 candidate went to verification. All were refuted, with the shape named: missing images and blank
  thumbnails were lazy images a full-page capture never scrolled to (all load after scrolling); console errors on every
  load were Sanity Live's CORS answer to a localhost origin (the preview and www origins are allowed); image 500s came
  from a stale local server; "menu clicks land on the wrong page" came from a dismiss click at coordinates the open
  panel covered (clicking outside closes the menus and every link lands on its own page). Confirmed and fixed: the case
  study closing heading lost its space on phones (P2).
- seo-aeo-geo-audit: before/after snapshot of all 186 routes — no JSON-LD type lost or gained, titles and canonicals
  unchanged except three team profiles (deliberate), one H1 per page, every FAQPage question visible on its page. Fixed
  during the gate: footer links to /cookies and /methodology, About links to team profiles, the crawler-readable 404,
  speakable summaries, blog FAQ links. No P0/P1 open.
- Fresh-agent review (clean context, Opus): no P0. P1 blog FAQ answers printed raw HTML → fixed. P1 the homepage chart
  read reached api.sanity.io on every view → cached with the case study tag. P1 the release depends on untracked files
  → the launch list covers every changed file except `.claude/launch.json` (local) and the V3-HANDOVER deletion (listed);
  `public/images/objects` is referenced by no launched code. P2 catch-all 404 sent an empty shell → removed. P2
  speakable selector matched nothing → restored. P3 error pages used `reset` → `retry`. Found by the gate after the
  review: the v11 pages drew CMS images straight from cdn.sanity.io (45 per /case-studies view) → routed through Vercel.

## Speed (Lighthouse 12, phone profile, preview against www on the same day)

| Page | Preview | Live v3 |
|---|---|---|
| / | 84, LCP 4.4 s | 82, LCP 4.8 s |
| /services/cro | 84, LCP 4.4 s | 83, LCP 4.7 s |
| /case-studies/genie-teacher-organic-growth | 98, LCP 2.1 s | 97, LCP 2.1 s |
| /about | 98, LCP 2.3 s | 81, LCP 5.2 s |
| /blog/ai-search-visibility-webinar-recap | 95, LCP 2.9 s | 75, LCP 10.0 s |

## Remaining P2/P3 (not blocking)

- A `notFound()` inside a site route (a deleted post) is drawn in the browser, as on v3.
- Inline editor: headline text beside its highlighted span is not editable (about 45 headings); service page copy, case
  study labels and some audit report sentences live in code. All carried over from v3.
- Contact shows arnel@loudface.co; footer and schema say hello@loudface.co.
- Testimonial videos stream from Sanity on play (23 MB for the three).
- Methodology has no stage rail on phones; lint errors remain in pre-existing forms and dev previews (CI does not lint).

Run by: Claude (Opus 5.5) in Arnel's session, with sub-agent lanes (Sonnet) and a fresh Opus reviewer.
