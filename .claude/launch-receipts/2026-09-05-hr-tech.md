# HR-tech launch receipt

Date: 2026-09-05. User approved the presented page with “go ahead”.
Release branch: `codex/hr-tech-launch`, isolated from `origin/main`.
Route: https://www.loudface.co/seo-for/hr-tech
Canonical Spine row: `2b3fc9c2-536a-471e-a8cb-db59d0416efb`.
Body SHA256: `3320c676e0457e3f30b7d23db7dcdfbfbc894a1eb72d65eba8ea7c31773d137a`.
Combined body/FAQ gate hash: `65cae833685f055279995c48d41355ca1cef4d79f0cce8e59d2686448ca75d13`.

## Gate evidence

- Design pick logged verbatim in `.claude/design/picks-log.jsonl`. No invented durable taste preference or rejected variant.
- Two fresh independent Full content verifiers: SHIP, 18/18 voice, 0 failed claims, 0 partials, 14 verified each. Reports in content-engine `.claude/research/loudface/hr-tech-service-page.launch-verify-{a,b}.md`. Both hashes reproduce.
- Both initially miscounted four/five-item factual lists as rhetorical triplets. Each re-read the exact rule and independently withdrew that finding. No rule was relaxed, no copy changed to secure those adjudications.
- Earlier genuine sentence-pattern issue fixed by a separate surgeon. Existing six FAQs copied exactly into the explicit gate sidecar. Four verify-stage gates and ship-stage gate PASS.
- Site-engineering `check.mjs --build`: 0 FAIL, 11 WARN, 15 PASS. Production build succeeded in this isolated worktree.
- Typecheck PASS. New route, hub, component, service data and sitemap ESLint PASS. Existing date-ticker/harvested hook lint errors remain outside the color-only edits; no logic changed there.
- Independent engineering reviewer `01a071b0-9039-76e2-b2a9-a4eb614aa40e`: no introduced P0/P1; body/FAQ parity, SSR, canonical, eager hero, quality82, hub ItemList, duplicate suppression and service link PASS. Its stale build-receipt note is resolved by this superseding receipt.
- Main visually checked 1440, 768 and 375px. No page-wide horizontal overflow. Buyer table intentionally scrolls within its container. FAQ expands. Buyer-question anchor works. Booking modal tested without submitting a booking in the build pass.
- Page-specific content is server-rendered: one H1, six FAQ answers in HTML and matching FAQPage schema. BreadcrumbList and Service schema are present.
- Automated sweep: local TLS required the Portless CA. Two image failures were parser false positives: sweep passed literal `&amp;` in optimized-image URLs. Browser-decoded URLs loaded with naturalWidth1080/900, with no broken page images. No checker was weakened.
- Local preview is loopback-only, not a public crawlable URL. Reviewer withdrew its preview-indexing P1 after checking resolution. Concise title/description lengths are accepted guidance warnings, not missing metadata.

## Scope and warnings

- Code-owned route, not a new Sanity document. CEIPAL proof remains website migration only, not AI performance or revenue.
- Added sitemap entry, industry hub card/ItemList entry, and one service FAQ link. The top HR listicle receives a pure anchor wrap only after production returns 200.
- Existing default-palette gate violations corrected narrowly in date-ticker and harvested components. Equivalent HR page colors use existing tokens. Remaining custom decorative colors preserve the approved design.
- Eleven existing repository warning classes remain: hex markup, focus, raw images, fetch TTL, ISR budget, raw links, README, TODO, global error page, tracked artifacts and package-manager pin. The new service anchor is HTML because that existing field is intentionally rendered as HTML. No unrelated feature rewrites.
- A shared session added proposal-chart commits to the original branch. This release cherry-picked only the HR build and transferred its named launch paths. Those proposal commits are excluded.
- Local build first rejected a node_modules symlink outside the worktree root. A local lockfile install fixed it; build then passed. No deployment config was changed for this workaround.

## Publication state at this receipt

All pre-publication gates passed. Merge/deploy and public render checks follow this commit. The Spine publication activity is the durable final receipt for the live URL, merge and retro-link transaction.
