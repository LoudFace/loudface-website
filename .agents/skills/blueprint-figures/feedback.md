# blueprint-figures — feedback ledger

The skill's memory. Append an entry after EVERY use where a human reacts to figures made with
this skill. Newest entries outrank older rules when they conflict — fold durable lessons back
into SKILL.md (see the self-improvement protocol) so rules and ledger never drift apart.

Entry format:
```
## YYYY-MM-DD · <surface / task>
MADE: <what was drawn>
VERDICT: "<the human's words>"
LESSON: <one durable line — or "none (confirmation)">
FOLDED INTO SKILL.md: yes/no (<which rule>)
```

---

## 2026-07-03 · LoudFace homepage problem section (origin session)
MADE: 3 typographic treatments of the 5-problem section (ledger rows / dark report panel / split checklist) — no graphics.
VERDICT: "doesnt look inspiring or interesting at all, just walls of text... attach a simple graphic to each item. graphics with similar style to makingsoftware.com"
LESSON: Items-with-figures beat items-as-text; the graphic IS the section, captions shrink to title + one line.
FOLDED INTO SKILL.md: yes (core premise + bans)

## 2026-07-03 · FIG.001–005 first figure pass
MADE: 5 plates (exploded homepage, locked pipeline, flat-traffic chart, AI answer sheet, leaking funnel).
VERDICT: judge caught invented "TRAFFIC IN — 10,000 / MO" annotation; user's standing law: real numbers only.
LESSON: Figure annotations use real project numbers or neutral qualitative labels ("GROWING", "NO LIFT") — never invented numerals, even illustrative ones.
FOLDED INTO SKILL.md: yes (drawing method #3, bans)

## 2026-07-03 · Fidelity pass from client's deep capture
MADE: refined all 5 plates to the measured spec (texture tile, ramp discipline, ants/flicker/settle as CSS).
VERDICT: "These graphics look pretty neat. I like them." + request to make this a self-improving skill.
LESSON: The measured mechanics (tile paper, single-stroke ramp, one flicker, CSS motion with reduced-motion kill) are the approved baseline — treat as the floor, not a suggestion.
FOLDED INTO SKILL.md: yes (whole system)

## 2026-07-03 · Skill exemplar extraction (self-healing note)
MADE: standalone exemplars/plates.html extracted from the shipped section.
VERDICT: first render lost all hatch fills + arrowheads — the shared <defs> (fx-arr marker, fx-hatch pattern) lived outside the extracted nodes.
LESSON: The shared SVG defs block must ship ONCE on every page using plates; its absence fails silently (hollow fills, bare leaders). Verify from the RENDER, not the code.
FOLDED INTO SKILL.md: yes (verify loop #3)

## 2026-07-03 · Skill eval round 1 (2 tasks, with-skill vs baseline)
MADE: pipeline + citation-gap figures, with-skill (8/8 spec checks) vs no-skill baselines (3/8, 4/8).
VERDICT: "the ones without the skill look slightly better for some reason... more creative and unique. However... the one with the skill has a different style that adheres closer to the inspiration [makingsoftware.com]. Even the graphics here look better than the ones in [the homepage plates]."
LESSON: Spec fidelity is the FLOOR, not the composition. The skill's discipline must not shrink ambition: compose like the best baselines did — show SCALE with element fields (a pool of many small units), run the story across the FULL canvas as one flow, encode states visually (muted -> active -> highlighted), add a small legend when states matter. Also: the bar moves — newer figures beat the shipped homepage plates, so exemplars must be refreshed from the latest wins.
FOLDED INTO SKILL.md: yes (drawing method — new ambition clause; exemplar refresh note)

## 2026-07-03 · Skill eval round 2 (citation figure, ambition rule added)
MADE: citation figure v2 — kept 8/8 style + added scale-field / full-canvas-flow / state-legend.
VERDICT: "looks better now. i still feel like the ones [on the homepage / makingsoftware.com] are much better."
LESSON: THE HIGHEST TIER IS DIMENSIONAL, NOT FLAT. The reference plates + the best homepage plate (exploded-homepage FIG.001) are ISOMETRIC / AXONOMETRIC exploded-view drawings of a CONSTRUCTED OBJECT — seen at an angle with real thickness, stacked Z-layers, parts pulling apart along dimension lines, dense mechanical detail. A flat 2D schematic (grid of flat squares, flat bars) reads as a wireframe/dashboard = the FLOOR. Default to drawing the concept as a real object in isometric/exploded perspective; flat is allowed only for genuinely-2D subjects (a chart, a timeline). Honest ceiling: the reference is hand-illustrated by a pro over months — dimensional drawing is the lever that closes most of the gap, not a guarantee of parity.
FOLDED INTO SKILL.md: yes (drawing method — dimensional-first archetype order + depth requirement)

## 2026-07-03 · Skill eval round 3 (citation figure v3 — dimensional cut)
MADE: AI-citation-gap plate rebuilt DIMENSIONALLY per rule #0 — an axonometric retrieval rig: an isometric receding field of page-card slabs (real top+front+right faces) = the open web; a 3D intake hopper + inclined ramp-belt lifting a few tinted candidate cards through a dashed retrieval gate; an iso ranker block scoring the top 2–3; and a STANDING answer slab with a depth face carrying 3 CITED rows + a flickering dashed "YOU · NOT CITED" slot; a slumped iso pile of never-retrieved cards falling below the gate. Story runs left→right on a RETRIEVE→RANK→CITE dimension rail. 12+ elements with depth, one flicker, ants only on the three flow lines, no invented numerals, detector clean (0/0). Studied ref-ms floppy/keyswitch/HDD crops directly before drawing to calibrate iso projection + slab thickness.
VERDICT: "yes this looks better now. still some visual improvements that can be done."
LESSON: CONFIRMED — the flat→dimensional cut is the lever that worked; rule #0 is validated, not provisional. Working iso recipe that landed: one dimetric basis (+X=(+26,+15), +Y=(-26,+15), height=up), every card/box = 3 faces in 3 ramp steps (top lightest → front mid → right darkest), labels parked in clear paper with leaders reaching in — NOT sitting on the object. Label-on-object collisions were the main cleanup cost; budget for it. "Still improvements" = not parity yet (see round 4 — the real source capture reveals the lattice-stamping technique that was still missing).
FOLDED INTO SKILL.md: yes (rule #0 marked confirmed; the "not provisional" note added)

## 2026-07-03 · Skill eval round 4 (client supplied the raw reference SVG source)
MADE: nothing new drawn yet — folded the client's SECOND deep capture (the raw inline `<svg>` source of six makingsoftware.com figures) into the skill.
VERDICT: "still some visual improvements that can be done. heres more json of the source site" + the actual figure SVGs.
LESSON: The named remaining gap between v3 and the reference is CONSTRUCTION, not just "be dimensional." The reference builds ALL its depth from ONE unit square run through ONE iso matrix `matrix(0.86603 0.5 -0.86603 0.5 tx ty)`, stamped dozens of times back-to-front into a dense lattice — where v3 hand-drew a few chunky boxes. That's why the reference reads as a real constructed object and hand-boxes read as blocks. Also surfaced from source: exact tile depth-ramp (display-p3 → --primary-*), reusable `#grid`/`#stripes` pattern fills, a fuller motion vocabulary (draw-on / travel / spin / wobble / mask-wipe / staggered settle), and cast-shadow tiles as the one sanctioned off-ramp tint (surface-*/0.2, ground plane only).
FOLDED INTO SKILL.md: yes (new file `references/source-svg-techniques.md` added to Step-0 load order; rule #0 now points at it for geometry; changelog (d))

## 2026-07-03 · Problem-section variants → A+B chosen → perfecting pass
MADE: 3 style variants × 2 subjects (A exploded-iso / B cutaway / C flat), then the full 5-plate problem section in the chosen A+B mix (001 iso, 002 cutaway, 003 iso-chart, 004 iso-citation, 005 cutaway).
VERDICT: variants → "A looks best, and i also like B". Full section → "the concept looks great, just a lot of small details that look off — texts overlap with the shapes, the z-index/layering looks wrong. how can we perfect this?"
LESSON: The concept/direction was right first try; what read as "sloppy" was PLACEMENT, not composition — hand-eyeballed label coords drifting onto shapes + non-strict draw order. Fix is mechanical, not taste: (1) label GUTTERS — every text in a L/R margin, object in the middle third, leaders reach in → text-on-shape impossible; (2) STRICT PAINT ORDER — shapes list then labels list, labels emitted LAST, faces depth-sorted; (3) per-plate ZOOM verify — crop each plate to ~800px and read alone, thumbnails hide 10px overlaps. Also: watch label length vs gutter width ("YOU · NOT CITED" clipped → "NOT CITED"; the caption carries the sentence). After applying: all 5 clean, 0 detector-critical.
FOLDED INTO SKILL.md: yes (new "Placement & z-order" section; verify-loop zoom step; changelog (e))

## 2026-07-04 · Z-order catch on FIG.001 (exploded stack)
MADE: the perfected 5-plate section; client zoomed FIG.001.
VERDICT: "notice how the stacking layering is reversed. the lowest tile is on top of the mid tile, but it should be reversed."
LESSON: My fresh (e) paint-order rule ("depth-sort by ix+iy") was right for an iso FLOOR but wrong for a vertical exploded STACK — that sorts by HEIGHT. A height-stack must paint BOTTOM-TO-TOP: the raised/upper slab is nearer the camera and must occlude the one below. General test when two parts overlap: "which is nearer the camera? → draw it later." (One-line fix: reverse the slab emission order.)
FOLDED INTO SKILL.md: yes (depth-key-depends-on-layout clause added to the paint-order rule; changelog (f))
ALSO CAUGHT (same view): screen-horizontal "content rows" I'd drawn on the exploded slabs looked weird — a flat line doesn't sit on a tilted iso face, it floats. LESSON: surface detail on an iso face must be iso-aligned (parallel to a face edge) or omitted. Removed them; folded into source-svg-techniques §1.

## 2026-07-04 · Meta-learning — "can we learn something to improve the skill?"
MADE: nothing new — stepped back on the pattern across the whole section.
VERDICT: "looks much better now, can we learn something from this to improve the skill?"
LESSON (the big one): the human kept catching my GEOMETRY bugs (reversed stacking, floating flat lines) because self-verify only checked DISCIPLINE (colour, flicker, numbers, gutters), never 3D CORRECTNESS — the human was my depth-buffer. And each fix was a prose rule a future agent would skim and re-break by hand. The durable fix for a recurring CLASS of defect is not another rule — it's (a) encode the rule as a correct-by-construction HELPER so it can't be hand-rolled wrong, and (b) add a VERIFY GATE that interrogates for exactly that defect. Applied: shipped references/iso-helpers.py (cube/floor/vstack/face_rows/gutters/emit-labels-last) + a "dimensional-correctness" verify pass (2b). Principle to carry forward: recurring defect → helper + gate, not just prose.
FOLDED INTO SKILL.md: yes (Step-0 item 6 → iso-helpers.py; verify 2b dimensional-correctness pass; changelog (g))

## 2026-07-04 · CORRECTION — the real lever was CONCRETENESS, not dimensionality (biggest lesson)
MADE: an all-isometric A+B rebuild of the 5 probvar-d plates.
VERDICT: "these here [the ORIGINAL probvar-d FIG.001–005] still LOOK much better, why cant we get back to this baseline quality?"
LESSON: I chased the WRONG variable for ~8 turns. The approved baseline plates are 4/5 FLAT yet clearly beat my iso rebuild — because they DEPICT THE ACTUAL OBJECT with dense legible detail: a browser wireframe exploded to show a missing layer; a flowchart with a ticket-stack + padlock + clock; a real chart with axes + flat-line-vs-hatched-wedge; a ranked answer-sheet UI fed by source docs; a hatched flowing funnel with leaks. My "dimensional beats flat" reads (rounds 2–3) mis-attributed "looks better" to PROJECTION; the true lever is CONCRETENESS + DETAIL + right-projection-per-concept (mostly flat). Corrected rule #0/#1/#2. PRACTICAL OUTCOME: keep the original probvar-d plates — they ARE the bar; the iso rebuild was a downgrade; do not ship it. META-META: when a human says "X looks better," identify WHICH variable actually differs (concreteness? detail? projection? composition?) before banking a rule — I overfit an entire direction off one abstract eval figure. The self-improving loop can bank the WRONG lesson confidently; a later human comparison is the only correction — build in "verify the variable" before generalizing.
FOLDED INTO SKILL.md: yes (rule #0 rewritten concrete-and-fit; rule #1 fit-first; rule #2 specific-detail; changelog (h))

## 2026-07-04 · TEST of the corrected skill — does concrete + flat actually work?
MADE: fresh CONCRETE FLAT rebuilds of the 3 flat concepts I'd wrongly forced into iso — 002 flowchart (nodes + ticket-stack + padlock + clock + blocked direct-publish arc), 003 line chart (axes + flat actual line vs hatched expected-wedge + redesign marker), 004 answer-sheet UI (prompt panel + numbered cited rows + not-cited slot + source docs feeding in + your-site-with-X). Shown beside the approved originals in corrected.html.
VERDICT: (client reaction pending) — self-assessment: all 3 landed AT baseline quality — concrete, detailed, on-concept, flat — a clear recovery from the abstract iso blocks. The corrected rule #0/#1/#2 produced the right output. 0 detector-critical.
LESSON: two LABEL defects recurred (long labels clipping the plate edge; a central-target label gutter-placed so its leader crossed the whole figure and landed on another box). Fixed by construction: hardened iso-helpers gL/gR to CLAMP label width (clip-proof), and banked the NEAR-TARGET rule (label a central target directly above/below it, never from a side gutter). Recurring-defect → helper + rule, applied again.
FOLDED INTO SKILL.md: yes (iso-helpers.py gL/gR clip-proof clamp + near-target note)

## 2026-07-04 · ROOT CAUSE — a text skill can't carry visual craft; make it reference-driven
MADE: fresh concrete flat rebuilds (again). Client: "the original still looks much better. what is causing this discrepancy? maybe the skill is not well set up ... include visual screenshots ... source svg/json copies ... a text skill alone cannot help well enough."
LESSON (the deepest of the whole arc): the discrepancy is CRAFT — proportion, spacing, detail DENSITY — which prose cannot transmit. And critically, the skill had never carried, and I had never read/adapted, the approved plates' actual SVG SOURCE; I regenerated from principles every time, which is inherently lossy (drifts on proportion; drops the signature details: sheared content grid, folded-corner ticket, stacked queue, filled chart area + tape baseline + axis numbers, two-bar+pill result rows, bezier funnel walls). The client's proposed fix is correct: the skill must CARRY THE SOURCE CODE + A SCREENSHOT as the spec and be ADAPTED FROM, not a prose rulebook. Applied: extracted exemplars/approved-plates.html (verbatim from probvar-d) + approved-plates.png; Step-0 item 3 = adapt-from-source; verify 2c = compare-render-to-screenshot; relaxed the over-strict no-numbers rule (it had stripped chart axes).
META-META: when repeated human comparisons keep saying "still worse," stop tuning prose rules — the MEDIUM may be wrong. Give the agent the artifact (code + image) to adapt, not a description of it. Also: I over-claimed "baseline quality" 3x while the human saw the gap — trust the human's eye over my self-assessment, and use the render-vs-reference comparison (2c) as the real gate.
FOLDED INTO SKILL.md: yes (Step-0 item 3 reference-driven; verify 2c; rule #3 numbers; changelog (i); new exemplars/approved-plates.{html,png})

## 2026-07-04 · VERDICT — corrected (reference-driven) skill VALIDATED
MADE: all 5 concepts skill-made (adapting each approved recipe) side-by-side vs the originals in all5.html; plus 2 new concepts earlier (growth-engine flowchart, homepage-anatomy).
VERDICT: "yesss, it finally looks good. is this a result of the skill?"
LESSON: Yes — attributable to the skill CHANGE (reference-driven: it now carries the approved source SVG + a screenshot and says "adapt from source, verify render against the image"). Honest scope stated to the client: for the exact 5, "adapt" ≈ faithfully reproduce the approved code, so part of that quality is legitimate REUSE, not from-scratch generation; the cleaner proof of generalization was the 2 NEW concepts built by adapting recipes. Bottom line the whole arc converged on: a text-only skill could not transmit visual craft (proportion/spacing/detail-density); changing the MEDIUM — carry the artifact (code + screenshot) and adapt from it — is what worked, not more prose rules. Also validated: trust the human's eye over agent self-assessment (I over-claimed "baseline" 3x before the render-vs-reference gate + the human caught the real gap each time).
FOLDED: n/a (validation of changelog c–i). Skill considered fixed for now.

## 2026-07-04 · QA — 7-archetype range test, per-plate zoom-QA (artifacts found + fixed)
MADE: 7 NEW visuals via the corrected skill, one per archetype — timeline/gantt, comparison matrix, gauge trio, retrieval flow, rising chart, exploded pricing-page (001's shear technique on a new subject), CRO cycle. Then a proper per-plate zoom-QA (crop each ~1.7x, read alone).
FOUND (3 real micro-artifacts; discipline + dimension had already passed): (C) the single flick pulse was a lone dot on ONE gauge's arc crown → read as a stray bubble; (D) the SCHEMA chip was jammed under the doc (text wider than chip, ~4px gap) → read as merged/overlapping; (E) the "ORGANIC + AI" endpoint label kissed the curve's terminal arrow. A, B, F, G clean.
FIXED: (C) gave every needle a value-dot at its tip, LCP's pulses — symmetric + intentional; (D) widened chip to text + dropped it clear of the doc with a real gap; (E) lifted the label ≥16px off the endpoint. Re-rendered → 0 detector critical.
LESSON (durable, folded into verify 2d): the discipline + dimensional passes miss a third class — MICRO-ARTIFACTS on repeated units / label-to-icon proximity / endpoint labels. Rules: a live marker on a SET must sit on all units and pulse one; a chip under an icon must be text-width + ≥10px gap; an endpoint label must clear its arrow by ≥16px.
META: this was a self-QA (agent's eye), not a human verdict — but the fixes are objective (measurable overlaps / asymmetry), so banked as construction rules, not taste. Range test doubles as generalization evidence: the reference-driven skill produced 7 DISTINCT archetypes at good quality, not just faithful copies of the approved 5.
FOLDED INTO SKILL.md: yes (verify step 2d; changelog (j)). Awaiting human verdict on the 7.

## 2026-07-04 · HUMAN caught what my zoom-QA missed — FIG.E hatch leak
AFTER I reported the 7 QA'd + "clean," client: "on FIG.E, the lined area seems to leak outside its line." Correct — the compounding hatch wedge was a straight-chord triangle (launch→endpoint), but the actual line is concave, so the straight top edge poked above the curve = hatch leaking past the line. Fixed by rebuilding the wedge polygon from the curve's own points.
LESSON: (1) new quirk class → verify 2d bullet: area fills must be bounded by the line's OWN point list, never a straight chord across a curved boundary. (2) META reinforced yet again: my per-plate zoom-QA still missed a real artifact the human's eye caught immediately. The render-vs-human gate is not optional — self-QA reduces defects but does not replace the human. Keep shipping to the human eye, keep banking what it catches.
FOLDED: verify 2d (area-fill bullet).

## 2026-07-04 · META-FIX — construction helper + a REAL adversarial fresh-eyes run
CONTEXT: human caught the E hatch leak my zoom-QA missed → "how did the skill miss this" → diagnosed the QA as checklist-bound (only catches named classes; detector is geometry-blind; I confirmation-scanned for the 3 quirks I'd already fixed and skipped the 4th). Client: "go ahead" = fix the meta-limit.
DID: (1) iso-helpers.py area_under()/area_between() — correct-by-construction chart fills bounded by the line's OWN points, so the straight-chord-across-a-curve leak can't be hand-written again (guarantee 6). (2) verify step 2e — an adversarial, checklist-INDEPENDENT cold-read (reverse-engineer each plate from intent), ideally a FRESH subagent that didn't draw it, + verify-each-flag discipline. (3) RAN it for real: a fresh general-purpose subagent, given only the 7 crops, told "assume bugs, geometry/craft only" → returned 6 flags.
RESULT: verified each flag against render+coords — ALL 6 were already-fixed (E) or resolution false-positives (A "collision"=18px gap; G "arrow-gap"=corner-meeting at 1.7x; C "needles wrong for GOOD"=all three sit in the 0..0.62 green band; D "chips unequal"=equal in code) or subjective (F double-border=featured-plan frame). 0 new real bugs. The 7 stand.
LESSON: a fresh adversarial eye OVER-flags at crop resolution — its output is a candidate set, not a fix list. Its real value is (a) forcing an intent-vs-render read the author's checklist-scan skips, (b) candidates you then VERIFY at higher zoom / in source numbers. Never apply an adversarial pass's flags blind (would have "fixed" 6 non-bugs and likely broken correct geometry). The construction helper is the durable fix for the specific class; the adversarial pass is the general (partial) fix for "always one human-catch behind."
FOLDED: verify 2e; changelog (k); iso-helpers area_under/area_between + guarantee 6.
