diff --git a/src/app/home-v3/HeroV3.tsx b/src/app/home-v3/HeroV3.tsx
index 13db128..79aab82 100644
--- a/src/app/home-v3/HeroV3.tsx
+++ b/src/app/home-v3/HeroV3.tsx
@@ -1,7 +1,7 @@
+import type { ReactNode } from 'react';
 import Image from 'next/image';
 import { CHIPS_HTML } from './_chips';
 import type { HomeImages } from './data';
-import type { HomeV3HeroContent } from '@/lib/content-utils';
 
 /**
  * HeroV3 — the "showroom floor" hero: copy + CTA + AI-answer chips on the left,
@@ -17,7 +17,7 @@ const CDN = 'https://cdn.sanity.io/images/xjjjqhgt/production/';
    sliced their stat card and layering off. */
 const CROP = '?w=1000&fm=webp&q=82';
 
-type Card = { slug: string; asset: string; alt: string; eager?: boolean; priority?: boolean };
+type Card = { slug: string; asset: string; metric: string; client: string; alt: string; eager?: boolean; priority?: boolean };
 
 /* Curated order, SEO/AEO-first (2026-09-01). The marquee used to lead with the
    web-build era (Montblanc, Radisson, Hoxhunt, LIQID, Eraser) which no longer
@@ -34,17 +34,17 @@ type Card = { slug: string; asset: string; alt: string; eager?: boolean; priorit
    result1 (7.2x clicks, 12.78%): a pill that contradicts the picture behind
    it reads as an error. Re-cut the composite before changing these. */
 const COL_A: Card[] = [
-  { slug: 'toku-ai-cited-pipeline', asset: 'cafcfa6fadc9ea6d1d38391eda626fd12ff5e5a0-2880x1800.png', alt: 'Toku AI search visibility case study by LoudFace', eager: true, priority: true },
-  { slug: 'delshad-legal-content-engine', asset: 'dcf12e21516b5edbe76587b2b38710d17e2d431e-2880x1800.png', alt: 'Delshad Legal content engine case study by LoudFace', eager: true },
-  { slug: 'genie-teacher-organic-growth', asset: '21db9f63b8c898d5ac57015bd2617156ad61091b-2880x1800.png', alt: 'Genie Teacher organic visibility case study by LoudFace' },
-  { slug: 'dimer-health', asset: '467a77e9756e7890f1c62874d3388937727c4c6e-2880x1800.png', alt: 'Dimer Health website built by LoudFace' },
+  { slug: 'toku-ai-cited-pipeline', asset: 'cafcfa6fadc9ea6d1d38391eda626fd12ff5e5a0-2880x1800.png', metric: '0 \u2192 97.8% AI visibility', client: 'Toku', alt: 'Toku AI search visibility case study by LoudFace', eager: true, priority: true },
+  { slug: 'delshad-legal-content-engine', asset: 'dcf12e21516b5edbe76587b2b38710d17e2d431e-2880x1800.png', metric: '32.7% AI share', client: 'Delshad Legal', alt: 'Delshad Legal content engine case study by LoudFace', eager: true },
+  { slug: 'genie-teacher-organic-growth', asset: '21db9f63b8c898d5ac57015bd2617156ad61091b-2880x1800.png', metric: '12.9% AI share', client: 'Genie Teacher', alt: 'Genie Teacher organic visibility case study by LoudFace' },
+  { slug: 'dimer-health', asset: '467a77e9756e7890f1c62874d3388937727c4c6e-2880x1800.png', metric: '288% conversion lift', client: 'Dimer Health', alt: 'Dimer Health website built by LoudFace' },
 ];
 
 const COL_B: Card[] = [
-  { slug: 'trademomentum-niche-aeo-organic-growth', asset: '3d31a25d5ec584328e198c4617f7e9fd7f3ccaed-2880x1800.png', alt: 'TradeMomentum organic growth case study by LoudFace', eager: true },
-  { slug: 'stealth-fintech-ai-visibility', asset: 'bbb33c1a5f465dd585e93ea11d0a6963f9f82870-2880x1800.png', alt: 'Stealth fintech AI visibility case study by LoudFace', eager: true },
-  { slug: 'loudface-aeo-case-study', asset: '776d1fdb8b4da0aa4566e62d79efacdecb24cca2-2880x1800.png', alt: 'LoudFace answer-engine optimisation case study' },
-  { slug: 'outbound-specialist', asset: '357651c0add5b9c0f1df95b591021decce87a8bc-2880x1800.png', alt: 'Outbound Specialist website built by LoudFace' },
+  { slug: 'trademomentum-niche-aeo-organic-growth', asset: '3d31a25d5ec584328e198c4617f7e9fd7f3ccaed-2880x1800.png', metric: '11.7x impressions', client: 'TradeMomentum', alt: 'TradeMomentum organic growth case study by LoudFace', eager: true },
+  { slug: 'stealth-fintech-ai-visibility', asset: 'bbb33c1a5f465dd585e93ea11d0a6963f9f82870-2880x1800.png', metric: '10.5% AI visibility', client: 'Stealth Fintech', alt: 'Stealth fintech AI visibility case study by LoudFace', eager: true },
+  { slug: 'loudface-aeo-case-study', asset: '776d1fdb8b4da0aa4566e62d79efacdecb24cca2-2880x1800.png', metric: '10.4% of AI answers', client: 'LoudFace', alt: 'LoudFace answer-engine optimisation case study' },
+  { slug: 'outbound-specialist', asset: '357651c0add5b9c0f1df95b591021decce87a8bc-2880x1800.png', metric: '$200K sales in 30 days', client: 'Outbound', alt: 'Outbound Specialist website built by LoudFace' },
 ];
 
 function srcFor(c: Card, images?: HomeImages) {
@@ -59,12 +59,43 @@ function srcFor(c: Card, images?: HomeImages) {
  * response-time line are deliberately identical across variants so a measured
  * difference is attributable to the argument and nothing else.
  *
- * Only the copy varies — the client-work marquee is shared. Copy itself lives
- * in content.control / content.test (src/data/content/homepage-v3.json).
+ * Only the copy varies — the client-work marquee is shared.
  */
 export type HeroVariant = 'control' | 'test';
 
-function Wcard({ c, images, work, dup = false }: { c: Card; images?: HomeImages; work: { metric: string; clientLabel: string }; dup?: boolean }) {
+const HERO_COPY: Record<HeroVariant, {
+  eyebrowLabel: string;
+  eyebrowNote: string;
+  headline: ReactNode;
+  sub: ReactNode;
+}> = {
+  control: {
+    eyebrowLabel: 'AI-native organic growth',
+    eyebrowNote: 'B2B SaaS',
+    headline: <>Get discovered across Google and AI search. Turn visibility into customers.</>,
+    sub: (
+      <>
+        LoudFace is a full-stack organic growth agency for B2B SaaS and fintech, with AI visibility measured per engine.
+        We run GEO, SEO, AEO, content, and conversion to get companies discovered across Google and AI search. We
+        implement across your stack when the program needs it.
+      </>
+    ),
+  },
+  test: {
+    eyebrowLabel: 'B2B SaaS',
+    eyebrowNote: '200+ sites shipped',
+    headline: <>Your buyers ask AI first.<br />Make sure it <span className="soft">names you.</span></>,
+    sub: (
+      <>
+        LoudFace is a full-stack organic growth agency for B2B SaaS and fintech, with AI visibility measured per engine.
+        Toku reached 97.8% visibility on its category&rsquo;s top AI-search prompt. We add delivery work across your
+        stack when it supports the program.
+      </>
+    ),
+  },
+};
+
+function Wcard({ c, images, dup = false }: { c: Card; images?: HomeImages; dup?: boolean }) {
   return (
     <article className="wcard" aria-hidden={dup || undefined}>
       {/* No browser chrome bar here: every composite already contains its own
@@ -89,32 +120,40 @@ function Wcard({ c, images, work, dup = false }: { c: Card; images?: HomeImages;
             : { loading: c.eager && !dup ? ('eager' as const) : ('lazy' as const) })}
         />
       </div>
-      <span className="rpill"><i></i><b>{work.metric}</b><span>{work.clientLabel}</span></span>
+      <span className="rpill"><i></i><b>{c.metric}</b><span>{c.client}</span></span>
     </article>
   );
 }
 
-function Wcol({ cards, work, variant, images }: { cards: Card[]; work: { metric: string; clientLabel: string }[]; variant: 'a' | 'b'; images?: HomeImages }) {
+function Wcol({ cards, variant, images }: { cards: Card[]; variant: 'a' | 'b'; images?: HomeImages }) {
   return (
     <div className={`wcol ${variant}`}>
       <div className="wtrack">
-        {cards.map((c, i) => <Wcard key={c.slug} c={c} work={work[i]} images={images} />)}
-        {cards.map((c, i) => <Wcard key={`${c.slug}-dup`} c={c} work={work[i]} images={images} dup />)}
+        {cards.map((c) => <Wcard key={c.client} c={c} images={images} />)}
+        {cards.map((c) => <Wcard key={`${c.client}-dup`} c={c} images={images} dup />)}
       </div>
     </div>
   );
 }
 
-export function HeroV3({
-  images,
-  variant = 'control',
-  content,
-}: {
+export type HeroCopy = {
+  eyebrowLabel: string;
+  eyebrowNote: string;
+  headline: ReactNode;
+  sub: ReactNode;
+};
+
+export function HeroV3({ images, variant = 'control', copyOverride }: {
   images?: HomeImages;
   variant?: HeroVariant;
-  content: HomeV3HeroContent;
-}) {
-  const copy = content[variant] ?? content.control;
+  /**
+   * Replaces the variant's copy outright. Used by the review routes to preview
+   * an argument that is not (yet) part of the running experiment; the live
+   * homepage never passes it, so the experiment stays the source of truth.
+   */
+  copyOverride?: HeroCopy;
+} = {}) {
+  const copy = copyOverride ?? HERO_COPY[variant] ?? HERO_COPY.control;
 
   return (
     <section className="hero" data-hero-variant={variant}>
@@ -123,27 +162,24 @@ export function HeroV3({
           <span className="hero-eyebrow rv"><b>{copy.eyebrowLabel}</b><em>{copy.eyebrowNote}</em></span>
           <h1 className="rv" style={{ transitionDelay: '.06s' }}>
             {copy.headline}
-            {copy.headlineBreak && <br />}
-            {copy.headlineHighlightPrefix}
-            {copy.headlineHighlight && <span className="soft">{copy.headlineHighlight}</span>}
           </h1>
           <p className="hero-sub rv" style={{ transitionDelay: '.12s' }}>
             {copy.sub}
           </p>
           <div className="hero-cta rv" style={{ transitionDelay: '.18s' }}>
-            <a href="#book" data-cal-trigger="" className="btn btn-white btn-lg">{copy.ctaText}</a>
-            <span className="slots"><i className="dot"></i>{copy.responseTime}</span>
+            <a href="#book" data-cal-trigger="" className="btn btn-white btn-lg">Book a strategy call</a>
+            <span className="slots"><i className="dot"></i>2h response time</span>
           </div>
           <div className="ai-row rv" style={{ transitionDelay: '.24s' }}>
-            <span className="ai-row-label">{copy.aiRowLabel}</span>
+            <span className="ai-row-label">See what AI says about us</span>
             <div className="ai-chips" dangerouslySetInnerHTML={{ __html: CHIPS_HTML }} />
           </div>
         </div>
 
         <div className="wall-zone rv" style={{ transitionDelay: '.14s' }} aria-label="Selected client work">
           <div className="wall">
-            <Wcol cards={COL_A} work={content.work.colA} variant="a" images={images} />
-            <Wcol cards={COL_B} work={content.work.colB} variant="b" images={images} />
+            <Wcol cards={COL_A} variant="a" images={images} />
+            <Wcol cards={COL_B} variant="b" images={images} />
           </div>
         </div>
       </div>
diff --git a/src/app/home-v3/ProblemSection.tsx b/src/app/home-v3/ProblemSection.tsx
index eeba7f7..b0c2247 100644
--- a/src/app/home-v3/ProblemSection.tsx
+++ b/src/app/home-v3/ProblemSection.tsx
@@ -1,6 +1,6 @@
+import type { ReactNode } from 'react';
 import { PLATE_DEFS, PLATES } from './_plates';
 import { BlueprintPlate } from './BlueprintPlate';
-import type { HomeV3ProblemContent } from '@/lib/content-utils';
 
 /**
  * ProblemSection — "Five ways your site is quietly costing you deals" on the
@@ -8,7 +8,23 @@ import type { HomeV3ProblemContent } from '@/lib/content-utils';
  * "if two of these sound familiar" band. PLATE_DEFS holds the shared SVG
  * <defs> (arrowhead marker + hatch pattern) included once for all plates.
  */
-export function ProblemSection({ content }: { content: HomeV3ProblemContent }) {
+export function ProblemSection({
+  plates = PLATES,
+  heading = <>Five ways your site is quietly costing you deals<span className="ghost">.</span></>,
+  ruleTag = 'FIG.001–005 · FIELD NOTES FROM 200+ BUILDS',
+  eyebrow = 'The problem',
+  bandText = 'If two of these sound familiar, the rest of this page is for you.',
+  showCta = true,
+}: {
+  /** Subset and order of the blueprint plates to show. */
+  plates?: typeof PLATES;
+  heading?: ReactNode;
+  ruleTag?: string;
+  eyebrow?: string;
+  bandText?: string;
+  /** The closing band's CTA. Off when a later section already owns the ask. */
+  showCta?: boolean;
+} = {}) {
   return (
     <section className="problem">
       {/* shared arrowhead + hatch defs — included once, referenced by every plate */}
@@ -16,31 +32,33 @@ export function ProblemSection({ content }: { content: HomeV3ProblemContent }) {
 
       <div className="container">
         <span className="eyebrow rv">
-          <i aria-hidden="true"></i>{content.eyebrow}
+          <i aria-hidden="true"></i>{eyebrow}
         </span>
         <h2 className="display rv" style={{ transitionDelay: '.04s' }}>
-          {content.headline}<span className="ghost">.</span>
+          {heading}
         </h2>
         <div className="light-rule rv" style={{ transitionDelay: '.08s' }} aria-hidden="true">
-          <span className="rule-tag">{content.ruleTag}</span>
+          <span className="rule-tag">{ruleTag}</span>
         </div>
 
         <div className="figgrid">
-          {PLATES.map((p, i) => (
-            <BlueprintPlate key={i} p={p} content={content.plates[i]} />
+          {plates.map((p, i) => (
+            <BlueprintPlate key={i} p={p} />
           ))}
         </div>
 
         <div className="pband rv" style={{ transitionDelay: '.08s' }}>
           <p>
-            <i aria-hidden="true"></i>{content.bandText}
+            <i aria-hidden="true"></i>{bandText}
           </p>
-          <a href="#book" data-cal-trigger="" className="pband-btn">
-            {content.bandCtaText}{' '}
-            <svg viewBox="0 0 13 13" fill="none" stroke="currentColor" strokeWidth="1.6" aria-hidden="true">
-              <path d="M1.5 6.5h10M8 2.5l4 4-4 4" />
-            </svg>
-          </a>
+          {showCta ? (
+            <a href="#book" data-cal-trigger="" className="pband-btn">
+              Book a strategy call{' '}
+              <svg viewBox="0 0 13 13" fill="none" stroke="currentColor" strokeWidth="1.6" aria-hidden="true">
+                <path d="M1.5 6.5h10M8 2.5l4 4-4 4" />
+              </svg>
+            </a>
+          ) : null}
         </div>
       </div>
     </section>
