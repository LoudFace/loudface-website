# Design wrap-up, 2026-09-26 — the run's task list

Arnel, 2026-09-26: "get the menus, cookie banners, design system page, and all of the other things done. Let's wrap
all the design up and get it ready for development."

Resume from this file after a context reset, not from memory. Tick a line only when it is checked on screen.

## Tasks
1. [x] Harvest: menus, cookie banner, phone menu, methodology → `design-lab/harvest/2026-09-26/chrome`, contact sheets sent
2. [x] Menus: v11 Services and Industries panels (all 11 industry pages), phone menu with the ink pill; the header
       shows them on `/dev-preview/home-v11*` only, so the live site is unchanged until go-live
3. [x] Cookie banner in v11, same copy and behaviour, v11 previews only
4. [x] Site chrome preview route + Paper board (menus open, banner, phone menu)
5. [x] Methodology v2 from the new harvest, copy moved to JSON unchanged; replaces the Paper board
6. [x] Independent design review (singles, audit set, chrome, methodology); fix what it finds
7. [x] Design system page: kit gains every part added since 25 Sep + chrome; ghost colour; re-import to Paper
8. [x] Phone boards: Paper page "Phone · v11", one 390 board per page type (28 boards, in the new file)
9. [x] Development handoff: `docs/v11-handoff.md` (route map, data, go-live steps, open questions); DESIGN.md,
       COMPONENTS.md, AGENTS.md banner updated
10. [x] `npm run build` (276 pages) + tsc clean + inline editor 591/591, 0 FAIL; memory updated; Wrap reply

## Notes
- Old Paper file 01M2K2HVAENQ1BS0B1917WV73N (full; history only). Current file 01M3ETYS4QMXF7CYH0VHZWF7J3.
- Desktop: row y=0 homepage + 9 services; row y=13000 everything else, x = 0 … 44820 (step 1660). Phone: y=0, step 470.
- Import: `scripts/design/paper-v11-import.mjs` (wraps the plugin scripts, adds the v11 flags and the object-position sweep).

## Independent review findings (2026-09-26, two design-reviewer passes) — fix list
Legend: [x] fixed · [~] needs Arnel (copy/facts) · [-] declined with reason

### Chrome
- [x] Cookie card: give "Accept all" and "Essential only" equal weight (both the same pill style)
- [x] Services menu: nine look-alike cells; group them (needs labels: check footer's service names)
- [x] Industries menu: mixes stages (Startups, AI startups, SaaS, B2B) with sectors; group "By sector" / "By stage"
- [-] Featured cards repeat things (low; Services promotes the lead offer; kept) (Services = homepage tile; Industries logos = row icons)
### Audit report (v11) and example data
- [x] Share-of-voice bars mix measures (Acme share 18% vs competitors' raw rates 72/65/48); plot one measure
- [x] Example data contradicts itself (rebuilt with the pipeline's own scoring) (#3 of 6 vs charts; 6 queries vs 10 rows; 5 tracked vs 3 charted; "by: claude")
- [x] Phone: landscape chart scrolls sideways; show a ranked bar list on phone
- [x] Engine cards: pill + repeated number on each; use one table/dot chart
- [x] Action plan: pill per row, Title Case, identical links; set as a signed plan sheet
- [x] Misses drawn faintest (2.3:1), engine labels 11.5px
- [x] Method section has no picture (folded into each phase head; copy question listed); "Three-phase" vs "all 4 phases" (copy: Arnel)
- [x] Peach alert box with fear copy beside the ChatGPT evidence
- [~] One name per measure (4–5 names today) (copy: Arnel)
- [x] Scatter: no ticks/values; aligns off grid
### Audit tool
- [~] Running: status line prints "[competitor]" brackets (live AuditProgress copy)
- [x] Running (phone): margin override removes side gutters
- [x] Start: re-check (capture bug was the shoot script); now shows the four steps ?screen=start capture; own picture (the four steps) instead of the landing's scorecard
### Apply
- [x] "Who reads it": four unnamed small faces; show at size with names or fewer
- [x] Phone: form 1,250px down; order intro, form, then how we hire
### 404
- [x] Link photos borrowed (restored the live page's four links, plain list; dropped my invented '50+' line) from service pages; plain link list
### Dividers
- [x] 1px ink rules (all v11 pages built since the service pages; svc.css kept) in report.css, careers.css, lost.css (and methodology): use --line
### Webinar
- [x] Past event still sells a seat (past state after hero.startsAt: recording via recap post, booking closing, no audit pitch); recording + recap state; disabled-looking CTA
- [x] Audit hero repeated below the closing (gone in the past state); cut
- [x] Arnel's cutout unframed (v11 portrait on indigo) next to framed photos
### Thank you
- [x] "Booked" card shows a fixed past slot as if it were the visitor's; drop the time
- [x] Phone H1 orphan "soon."; balance
- [x] Numbered step markers; drop (NextSteps, shared with contact)
- [x] Case study card repeats Genie (Dimer now); insights card kept: the blog index shows CMS covers too; insights card off-system
### AI instructions
- [x] Hero picture is the homepage's Toku answer; own ChatWindow ("What is LoudFace?")
- [~] Facts repeated three times (live copy; Arnel); FAQ now opens the first answer only
- [-] Toku result without a chart (no Toku series in the data layer; the quote carries the number)
### Partners
- [x] Form help text inherits 17px copy style; copy column empty 1,000px
- [x] Running-total bars flatten from month 10 (flex shrink)
- [-] d-05 repeats the 10% offer (live copy with a tracked CTA; not two DOM sections in a row); cut
- [x] Numbered "Step 1–4"; drop
- [x] Button arrow touches label (.v11-btn gap 8px); terrain removed from the partners closing (.v11-btn gap); lone terrain peak
### Team
- [-] Results section is the homepage's (kept as the team's published results); retitle to his accounts or cut
- [~] Colleague titles contradict (profiles follow Arnel's 25 Sep rule; the homepage team section is the odd one out: question for Arnel) homepage roles (Arnel's rule said "Lead SEO, AEO & GEO Specialist" — check)
- [x] Portrait column ends early (sticky portrait, phone rail); Title Case keywords come from the CMS (no transforms); Title Case keywords; phone orphan colleague
### Legal
- [x] Phone: cookie table cuts off; stack rows
- [x] Consent button old style; v11 pill
### Audit landing
- [~] Section repeats the hero's list (live copy: both lists are on the live page)
### Cross-page
- [-] Light hero followed by white logo strip (the strip reads as part of the hero on the approved pages; making it warm puts two warms together) (two whites); logo strip on warm
- [x] Text inside pictures 11.5px (12.5px); engine names stay icon-only on phone (four known marks in 44px columns); 12.5px minimum; keep engine names on phone

## Paper file (2026-09-26)
The file "LoudFace Website" hit Paper's size limit mid-import ("Your file is too large. Further changes will result in
data loss"); writes stopped there. All current boards rebuilt from code in the new file "LoudFace Website · v11"
(01M3ETYS4QMXF7CYH0VHZWF7J3): Desktop (p-4-0), Phone (p-2-0), Design system (p-3-0), cover on Page 1.

## Final pass on the new file (2026-09-26, section by section)
- [x] Report: the "50%" tick wrapped in Paper (nowrap on every axis label; board patched, code fixed)
- [x] Report and audit windows on a phone: the audit line crowded the title; the bar keeps the title only (as the homepage's sheets)
- [x] Audit landing example disagreed with the example report (18%, #3, 3/4; 8 of 20 hits under a 35% label; bars mixing two measures): now the report's own Acme Corp numbers, queries and share of voice (also on the methodology start section and the kit)
- [x] Methodology chart legend: an empty square beside "Blended" read as a checkbox; gone
- [x] Blog covers over a ranked list: the sheet clipped the title's third line in Paper; two lines over a list or comparison
- [x] "What happens next": the example call said "Thu 25 Sep" (already past); now "Thursday · 14:00", and no time on /thank-you; the contact card's day and times come from JSON (the component no longer splits the copy)
